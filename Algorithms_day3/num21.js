function solution(s) {
    return s
        .split(" ")
        .map(x => x.split(''))
            // .map((e, index) => index % 2 === 0 ? e.toUpperCase() : e.toLowerCase())
            //     .join(''))
        // .join(' ')
        
         // join() 메서드는 배열의 모든 요소를 연결해 하나의 문자열로 만듭니다.
}
// forEach 문을 써도 될 것 같다